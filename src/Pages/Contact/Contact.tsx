// import { useContext } from "react";
// import { useTranslation } from "react-i18next";
// import MyContext from "../../context/Context";
import StarryBackground from "../../components/universe/Universe";

const Contact = () => {
  // const { language } = useContext(MyContext) || {}; // Asegúrate de manejar el valor undefined
  // const { t } = useTranslation();

  return (
    <div>
      <StarryBackground />
    </div>
  );
};

export default Contact;
